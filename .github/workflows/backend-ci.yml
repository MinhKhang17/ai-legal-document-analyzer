name: Backend CI

on:
  pull_request:
    paths:
      - 'backend/**'
      - '.github/workflows/backend-ci.yml'

permissions:
  contents: read

jobs:
  backend-ci:
    runs-on: ubuntu-latest
    steps:
      - name: Placeholder step
        run: echo "Backend CI placeholder - add Maven verify pipeline steps."

[pytest]
testpaths = tests
python_files = test_*.py
python_classes = Test*
python_functions = test_*
addopts = -ra -q
markers =
    neo4j: tests that require a running Neo4j instance
    llm: tests that require a configured LLM (Gemini) and network access
